#Avery bowman, multiple page notes

from calcs import add as x #only omports functions not variables
                    # "*" prints everything instead of "add"

print (x(4,6))

def info():
         return "andrew", 35, "orange"
name, age, color = info()

#if __name__ == "main"    only write this if it isnt on the main, NOT ON THE PAGE
   #add()
   #sub()
   #div()
   #mult()